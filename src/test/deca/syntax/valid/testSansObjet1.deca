// test non-object syntax
// add two integer and increment them each iteration
// Result : OK (for all tests, test_lex|test_synt|test_context)

{
  int x;
  int y;
  int z;
  int i = 0;
  x = 2;
  y = 6;
  while( i < 3 ){
     z = x + y;
     x = x + 1;
     y = y + 1;
     i = i + 1;
  }
}
